{
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "features": {},
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends tabix git-lfs man-db manpages && sudo git lfs install --system && sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/*"
}
